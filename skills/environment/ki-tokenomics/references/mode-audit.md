# Mode AUDIT

Run `ki repo audit --skill ki-tokenomics` to validate the selected repository's portable tokenomics policy. Compose it with the configured runtime adapter to collect runtime evidence. Report estimates, attribution, and owner routing separately; do not treat estimates as billing.
