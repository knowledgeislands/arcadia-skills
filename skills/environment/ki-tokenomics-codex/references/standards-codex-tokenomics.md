# Codex tokenomics evidence

Use only documented Codex instruction, configuration, skill, MCP, memory, and subagent surfaces, bounded to the selected repository and the user's documented Codex configuration. Report structural facts and names only; do not emit configuration values that may contain secrets.

The standard does not infer billing, exact token counts, tool-schema weight, compaction totals, or transcript metrics. Each is explicitly NOT_APPLICABLE until Codex exposes a documented, safe evidence contract.

Configuration and artifact changes are report-only. No safe item-owned write is currently proven.
